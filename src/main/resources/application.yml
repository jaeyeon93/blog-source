spring.application.name: blog-application

spring:
  jpa:
    properties:
      hibernate.hibernate.format_sql: true
    show-sql: true

blog-source:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    jdbc-url: jdbc:mysql://localhost:3306/blog_test
    username: test
    password: test

